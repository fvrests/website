---
export interface Props {
	Element?: string;
	id?: string;
}

const { Element = 'p', id = undefined } = Astro.props;
---

<Element id={id} class="mb-4 text-xl font-bold text-f-high tracking-tighter">
	<slot />
</Element>
