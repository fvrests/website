---
export interface Props {
	Element?: string;
	id?: string;
}

const { Element = 'p', id = undefined } = Astro.props;
---

<Element id={id} class="mb-2 text-base tracking-tight font-bold text-f-high">
	<slot />
</Element>
